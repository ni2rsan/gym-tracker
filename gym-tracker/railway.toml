[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npx prisma migrate deploy && node_modules/.bin/next start"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
